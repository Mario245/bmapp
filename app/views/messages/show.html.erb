<p id="notice"><%= notice %></p>

<p>
  <strong>To:</strong>
  <%= @message.to %>
</p>

<p>
  <strong>From:</strong>
  <%= @message.from %>
</p>

<p>
  <strong>Body:</strong>
  <%= @message.body %>
</p>

<p>
  <strong>Category:</strong>
  <%= @message.category %>
</p>

<%= link_to 'Edit', edit_building_message_path(@building, @message) %> 
<%= link_to "Delete Work Request", (@building, @message), :method => :delete,
             :confirm => "Are you sure you want to delete this work request?" %>
<%= link_to 'Back', messages_path %>
